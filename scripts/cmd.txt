gcloud artifacts repositories create gcr.io \
    --repository-format=docker \
    --location=us \
    --description="default" \
    --async \
    --disable-vulnerability-scanning

gcloud artifacts repositories list

gcloud auth configure-docker gcr.io --quiet

# docker buildx build --platform linux/amd64,linux/arm64  --push --tag "${IMAGE_URI}" .
# ERROR: failed to build: Multi-platform build is not supported for the docker driver.
# Switch to a different driver, or turn on the containerd image store, and try again.

docker build --platform linux/amd64 --tag "${IMAGE_URI}" .
docker push "${IMAGE_URI}"

docker tag "${IMAGE_URI}" "${IMAGE_URI}:3"
docker push "${IMAGE_URI}:3"

docker run -it --rm "${IMAGE_URI}"
